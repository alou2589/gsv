{% extends './admin/adminbase.html.twig' %}

{% block body %}
<div class="row">
<div class="col-md-12 col-sm-12 ">
  <div class="x_panel">
    <div class="x_title">
      <h2>Départements</h2>
      <ul class="nav navbar-right panel_toolbox">
        <li>
            <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
        </li>
        <li>
            <a href="#" title="Upload Excel" ><i class="fas fa-fw fa-file-excel-o text-success"></i></a>
        </li> 
        <li>
            <a href="{{ path('app_admin_departements_new') }}" title="Ajout département" ><i class="fas fa-fw fa-plus-circle"></i></a>
        </li> 
        <li>
            <a class="close-link"><i class="fa fa-close"></i></a>
        </li>
      </ul>
      <div class="clearfix"></div>
    </div>
    <div class="x_content">
      <div class="row">
        <div class="col-sm-12">
          <div class="card-box table-responsive">
            <p class="text-muted font-13 m-b-30">
              Liste des departements
            </p>
            <table id="datatable-buttons" class="table table-striped table-bordered" style="width:100%">
              <thead>
                  <tr>
                      <th class="text-center">Id</th>
                      <th class="text-center">Département</th>
                      <th class="text-center">Informations</th>
                  </tr>
              </thead>
              <tbody>
              {% for departement in departements %}
                  <tr>
                      <td class="text-center">{{ departement.id }}</td>
                      <td class="text-center">{{ departement.nomDepartement }}</td>
                      <td class="text-center">
                        <div class="row">
                            <div class="col-md-8 mx-auto">
                                <a href="{{ path('app_admin_departements_show', {'id': departement.id}) }}"><i class="fas fa-eye text-info" title="Détails"></i></a>
                                <a href="{{ path('app_admin_departements_edit', {'id': departement.id}) }}"><i class="fas fa-edit text-warning" title="Modifier"></i></a>
                            </div>
                        </div>
                      </td>
                  </tr>
              {% else %}
                  <tr>
                    <td class="text-center" colspan="3">
                        <a href="{{ path('app_admin_departements_new') }}" title="Ajout departement" >Ajouter <i class="fas fa-fw fa-plus-circle"></i></a>
                    </td>
                  </tr>
              {% endfor %}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>

{% endblock %}




