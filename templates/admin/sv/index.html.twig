{% extends './admin/adminbase.html.twig' %}

{% block title %}Statut | {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
      <div class="col-md-12 col-sm-12 ">
        <div class="x_panel">
          <div class="x_title">
            <h2 class="text-success">Statut</h2>
            <ul class="nav navbar-right panel_toolbox">
              <li>
                  <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
              </li>
              <li>
                  <a href="{{ path('app_admin_sv_new') }}" title="Ajout Statut" ><i class="fas fa-fw fa-plus-circle"></i></a>
              </li>
              <li>
                  <a class="close-link"><i class="fa fa-close"></i></a>
              </li>
            </ul>
            <div class="clearfix"></div>
          </div>
          <div class="x_content">
            <div class="row">
              <div class="col-sm-12">
                <div class="card-box table-responsive">
                  <p class="text-muted font-13 m-b-30">
                    Liste des statuts
                  </p>
                  <table id="datatable-buttons" class="table table-striped table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th class="text-center">Id</th>
                            <th class="text-center">Volontaire</th>
                            <th class="text-center">Matricule</th>
                            <th class="text-center">Date Recrutement</th>
                            <th class="text-center">Information</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for statut_volontaire in statut_volontaires %}
                      <tr>
                        <td class="text-center">{{ statut_volontaire.id }}</td>
                        <td class="text-center">{{statut_volontaire.volontaire.prenom}} {{ statut_volontaire.volontaire.nom}} </td>
                        <td class="text-center">{{statut_volontaire.matricule}}</td>
                        <td class="text-center">{{ statut_volontaire.dateRecrutement ? statut_volontaire.dateRecrutement|date('d-m-Y') : '' }}</td>
                        <td class="text-center">
                            <div class="row">
                              <div class="col-sm-6 mx-auto">
                                <a href="{{ path('app_admin_sv_show', {'id': statut_volontaire.id}) }}"><i class="fas fa-eye text-info" title="Détails"></i></a>
                                <a href="{{ path('app_admin_sv_edit', {'id': statut_volontaire.id}) }}"><i class="fas fa-edit text-warning" title="Modifier"></i></a>
                              </div>
                            </div>
                        </td>
                      </tr>
                    {% else %}
                        <tr>
                          <td class="text-center" colspan="5">
                            <a href="{{ path('app_admin_sv_new') }}" title="Ajout Agent" >Ajouter <i class="fas fa-fw fa-plus-circle"></i></a>
                          </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
{% endblock %}
